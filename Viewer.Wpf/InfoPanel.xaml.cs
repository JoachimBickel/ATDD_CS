using System.Windows.Controls;

namespace Viewer.Wpf;

// The model-info side panel: pure bindings, no logic.
public partial class InfoPanel : UserControl
{
    public InfoPanel()
    {
        InitializeComponent();
    }
}